/*
 * Here is where you will create a function that will add the user created to your servers.
 * If you return false, no user will be created and the button will not show up.
 * Otherwise the parameters are going to be (String: username, String: password);
*/

module.exports = false;
